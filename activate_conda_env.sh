# `conda activate talktuner-gpu` does not run with error "CondaError: Run 'conda init' before 'conda activate'"
# use the below command as an alternative to activate the conda environment.
# Note: you need to run this in terminal, not in the script.
source /opt/conda/etc/profile.d/conda.sh && conda activate talktuner-gpu && python --version